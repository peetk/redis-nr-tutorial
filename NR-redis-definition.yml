name: com.NewRelic.redis
description: Reports status and metrics for redis service
protocol_version: 1
os: linux

commands:
  metrics:
    command:
      - ./bin/NR-redis
      - --metrics
    interval: 15

  #inventory:
  #  command:
  #    - ./bin/NR-redis
  #    - --inventory
  #  prefix: config/NR-redis
  #  interval: 15
